
{% extends "BZUserBundle::layout.html.twig"%}

{% block content_body %}
<form  class="ui-draggable" style="position: relative; left: 20%; top: 20%;margin-top: 5%;" action="{{ path('compte_agent')}}" method="post" {{ form_enctype(form) }} >
<div class="col-lg-6">
    <div class="panel panel-info">
        <div class="panel-heading" style="text-align: center;text-shadow: 0px 0.5px 0.5px black;font-size: 13px;color: black;">
            MON COMPTE
        </div>
        <div class="panel-body">
          
          {{ form_widget(form) }}
        </div>
        <div class="panel-footer">
            <a type="button" class="btn btn-default btn-xs btn-line " href="{{ path('page_accueil')}}"><i class="icon icon-arrow-left"> </i> Retour</a>
                  <button type="submit" class="btn btn-info btn-xs btn-line" >Mettre à jour</button>
                  <span class='tachat_img hide'><img src="{{ asset('/images/712.GIF') }}" alt="Second slide" class='img-circle' style="height: 20px;width: 20px;"> Enregistrement ...</span>
        </div>
    </div>
</div>
{{ form_rest(form) }}
</form>
{% endblock content_body %}
{% block parentScript %}
<script>
         $('form').draggable();
</script>
{% endblock parentScript %}